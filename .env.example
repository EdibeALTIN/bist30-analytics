# Frontend (Vite) — kopyalayın: cp .env.example .env.local
# Lokal geliştirme
VITE_API_URL=http://localhost:8000

# Production (Vercel Dashboard → Settings → Environment Variables)
# VITE_API_URL=https://bist30-api.onrender.com
